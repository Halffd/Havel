^!g => {
  if mode.get() == "gaming" {
    mode.set("default")
  } else {
    mode.set("gaming")
  }
  gui.notify("Mode Changed", "Active mode: " + mode.get())
}

^!r => {
  config.reload()
}

!Esc => {
  app.quit()
}
