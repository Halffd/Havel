fn build_one(input, output) {
    ok = compiler.build(input, output)
    if ok: true
    else {
        print("compile failed")
        print(input)
        false

    }
}
fn main(args) {
    if args.len < 2 {
        print("usage: compiler_driver.hv <in.hv> <out.hvc>")
        2
    }
    else {
        ok = build_one(args[0], args[1])
        if ok: 0
        else: 1

    }
}