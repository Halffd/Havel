// smoke: regex-split = 3
use regex
arr = regex_split(",", "a,b,c")
val __result = arr.len
if __result != 3 {
 print("FAIL regex-split: expected 3, got " + str(__result))
 process.exit(255)
}
return __result
