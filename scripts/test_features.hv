// Test script for new Havel features

print("=== Testing Havel Features ===")

// Test 1: Type system - struct definition
print("\n1. Testing struct definition:")
struct Vec2 {
    x
    y
}
print("   Vec2 struct defined successfully")

// Test 2: Type system - enum definition  
print("\n2. Testing enum definition:")
enum Color {
    Red
    Green
    Blue
}
print("   Color enum defined successfully")

// Test 3: HTTP module
print("\n3. Testing HTTP module:")
response = http.get("https://httpbin.org/get")
if (response && response.ok) {
    print("   HTTP GET successful: status " + response.statusCode)
} else {
    print("   HTTP module available (network may be unavailable in test)")
}

// Test 4: Mouse functions (these require running environment)
print("\n4. Mouse functions available:")
print("   - mouse.click(button) - Fixed to use proper button codes")
print("   - mouse.moveTo(x, y) - Fixed with coordinate clamping")
print("   - mouse.move(dx, dy) - Relative movement")

print("\n=== All tests completed ===")
