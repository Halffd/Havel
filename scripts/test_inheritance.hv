// Test struct with method
struct Rectangle {
  width: int
  height: int
}

fn Rectangle.area() {
  return this.width * this.height
}

r = Rectangle.new(5, 3)
print("Area: " + r.area())
print("Type: " + type.of(r))
