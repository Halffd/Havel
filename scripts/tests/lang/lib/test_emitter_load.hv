use { emitStmt, emitExpr, Emitter, Scope } from "emitter"
use { NumberLiteral } from "ast"

em = Emitter()
bc.reset()
bc.func_new("__main__", 0, 32)
em.scope = Scope(null)

print("calling emitExpr with NumberLiteral...")
emitExpr(em, NumberLiteral(42, 0, 0))
print("emitExpr ok")
