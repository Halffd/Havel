exists = fs.exists("/tmp")
print("exists: ")
print(exists)

files = fs.readDir("/tmp")
print(f"files count: {files.len}")
txt = f"Date: {time.date()}"
fh = fs.open("/tmp/date.log", "w+")
fh.write(txt)
fh.seek(0)
cont = fh.read()
print cont
fh.close()
tl = fs.write("/tmp/test.log", """[INFO] starting
[ERROR] Error  
[INFO] Quitting""")
print tl.path
for f in fs.readDir("~"){
     if f.isDir() { fs.touch(f"{f.path}/1.txt") }
    print(f.extension)
}