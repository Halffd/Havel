#include "BrightnessService.hpp"
#include "../utils/Logger.hpp"

namespace havel::host {

// BrightnessService implementation is in the header (header-only for simplicity)
// The BrightnessManager from core does the actual work

} // namespace havel::host
