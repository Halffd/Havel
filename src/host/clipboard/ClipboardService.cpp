/*
 * ClipboardService.cpp
 *
 * Clipboard service - combines core + history.
 * Implementation is in base classes (Clipboard, HistoryClipboard).
 */
#include "ClipboardService.hpp"

namespace havel::host {

// ClipboardService inherits all implementation from Clipboard and HistoryClipboard
// No additional implementation needed - composition is complete

} // namespace havel::host
