/*
 * MediaService.hpp - STUBBED (interpreter removed)
 */
#pragma once

#include <string>
#include <vector>

namespace havel::host {

// STUBBED - interpreter removed
class MediaService {
public:
    MediaService() = default;
};

} // namespace havel::host
