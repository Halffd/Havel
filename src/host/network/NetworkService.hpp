/*
 * NetworkService.hpp - STUBBED (interpreter removed)
 */
#pragma once

#include <string>
#include <vector>

namespace havel::host {

// STUBBED - interpreter removed
class NetworkService {
public:
    NetworkService() = default;
};

} // namespace havel::host
