/*
 * TimerService.hpp - STUBBED (interpreter removed)
 */
#pragma once

#include <string>
#include <vector>

namespace havel::host {

// STUBBED - interpreter removed
class TimerService {
public:
    TimerService() = default;
};

} // namespace havel::host
