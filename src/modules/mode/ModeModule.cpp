/*
 * ModeModule.cpp - STUBBED (interpreter removed)
 * Mode module requires migration to bytecode VM
 */
#include "ModeModule.hpp"

namespace havel::modules {

// STUBBED - Mode module requires migration to bytecode VM
void registerModeModule() {
}

} // namespace havel::modules
