print("Testing detector functions availability:");
print("detectDisplay:", detectDisplay != null ? "AVAILABLE" : "NOT AVAILABLE");
print("detectMonitorConfig:", detectMonitorConfig != null ? "AVAILABLE" : "NOT AVAILABLE");
print("detectWindowManager:", detectWindowManager != null ? "AVAILABLE" : "NOT AVAILABLE");
print("detectSystem:", detectSystem != null ? "AVAILABLE" : "NOT AVAILABLE");

print("\nTesting filemanager functions:");
print("filemanager:", filemanager != null ? "AVAILABLE" : "NOT AVAILABLE");
if (filemanager) {
    print("filemanager.read:", filemanager.read != null ? "AVAILABLE" : "NOT AVAILABLE");
    print("filemanager.File:", filemanager.File != null ? "AVAILABLE" : "NOT AVAILABLE");
}
