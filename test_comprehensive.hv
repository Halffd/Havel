print("=== Comprehensive Module Test ===");

// Test basic functionality
print("print function:", print != null ? "WORKS" : "FAILS");

// Test mode module (should work)
print("mode module:", mode != null ? "WORKS" : "FAILS");
if (mode) {
    print("mode.get():", mode.get());
}

// Test config module (should work)  
print("config module:", config != null ? "WORKS" : "FAILS");
if (config) {
    print("config.get test:", config.get("Test.Value", "default"));
}

// Test media modules (should fail)
print("media module:", media != null ? "WORKS" : "FAILS");
print("mpvcontroller module:", mpvcontroller != null ? "WORKS" : "FAILS");

// Test debug variables
print("standard_library_completed:", standard_library_completed != null ? "YES" : "NO");
print("media_builtins_called:", media_builtins_called != null ? "YES" : "NO");

print("=== Test Complete ===");
