print("Testing detector functions:");

print("detectSystem results:");
print("OS:", detectSystem().os);
print("Window manager:", detectSystem().windowManager);
print("Display protocol:", detectSystem().displayProtocol);

print("detectWindowManager results:");
print("Name:", detectWindowManager().name);
print("Is Wayland:", detectWindowManager().isWayland);
print("Is X11:", detectWindowManager().isX11);

print("detectDisplay results:");
print("Count:", detectDisplay().count);
print("Type:", detectDisplay().type);

print("detectMonitorConfig results:");
print("Total monitors:", detectMonitorConfig().totalMonitors);
print("Session type:", detectMonitorConfig().sessionType);
