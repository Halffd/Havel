// Hotkey Conversion Test - Batch 1
// Converting first 3 hotkeys from HotkeyManager.cpp

// 1. Mode toggle (^!g)
^!g => {
    let currentMode = mode.get()
    let newMode = currentMode == "gaming" ? "default" : "gaming"
    mode.set(newMode)
    print "Mode changed to: " + newMode
}

// 2. Reload configuration (^!r)  
^!r => {
    print "Reloading configuration"
    // This would need a config.reload() function
    // For now, just print
}

// 3. Quit application (!Esc)
!Esc => {
    print "Quitting application"
    // This would need an app.quit() function
    // For now, just print
}