print("Testing use statement flattening...");

// Before use - should require module.method
print("Before use:");
print("io module available:", io != null ? "YES" : "NO");
if (io) {
    print("io.mouseMove available:", io.mouseMove != null ? "YES" : "NO");
}

// Use the modules to flatten their functions
use io, media, filemanager

// After use - should have direct access
print("After use:");
print("mouseMove available:", mouseMove != null ? "YES" : "NO");
print("play available:", play != null ? "YES" : "NO");
print("read available:", read != null ? "YES" : "NO");

// Test direct function calls
print("Testing direct calls:");
print("Writing to file...");
read("/tmp/use_test.txt", "Hello from use statement!");
print("Reading file content:", read("/tmp/use_test.txt"));
