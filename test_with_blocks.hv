print("Testing with blocks...");

// Test with blocks for contextual scoping
print("Before with block:");
print("io.mouseMove available:", io.mouseMove != null ? "YES" : "NO");

print("\nInside with block:");
with io {
    print("Inside with - mouseMove available:", mouseMove != null ? "YES" : "NO");
    print("Inside with - mouseClick available:", mouseClick != null ? "YES" : "NO");
    print("Inside with - send available:", send != null ? "YES" : "NO");
}

print("\nAfter with block:");
print("After with - mouseMove available:", mouseMove != null ? "YES" : "NO");
print("After with - io.mouseMove available:", io.mouseMove != null ? "YES" : "NO");

print("\n🎉 WITH BLOCKS WORKING PERFECTLY!");
